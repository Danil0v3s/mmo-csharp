# Parity Roadmap — Timeline & pick-order (vertical rebuild)

Companion to [README.md](README.md). The README is the **what**; this is the
**in-what-order**. Each ticket is a vertical slice (one playable capability, all layers).
The loop / a contributor takes the **first ticket in pick-order whose `Depends on:` are
all in `done/`**.

Old per-layer sequencing + the full historical progress log are archived in
[`_archive/TIMELINE-history.md`](_archive/TIMELINE-history.md).

## Standing directives

- **Gameplay first.** Phase 2 capabilities are the biggest player-facing win and where
  "done ≠ playable" hurt most.
- **Combat last, scripting truly last** (user pivot, persisted in memory).
- **Each ✅ must be playable end-to-end and survive logout** — not "service exists".

## Phase order

```
Phase A  gameplay/   ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓   playable capabilities (16)   ← do these
Phase B  infra/      ▓▓▓▓▓▓▓▓             small vertical features (8)  ← parallel anytime
Phase B  mobai/      ▓▓                   mob AI (2)                   ← parallel anytime
Phase C  status/     ▓▓▓▓                 SC depth (4)
Phase D  skills/     ▓▓▓▓▓▓▓▓▓            per-family depth (9)
Phase E  combat/     ▓▓▓▓▓                damage formula tail (5)      ← combat last
Phase F  scripting/  ▓▓▓▓▓▓▓              NPC runtime + content (7)    ← truly last
```

## Pick-order (Phase A — gameplay, do top-to-bottom)

Ordered by leverage (most-used capabilities + dependency-free first):

1. **GP-MAIL** — most-used social feature; the mail service already has real transfer logic, needs the RODEX packet set + persistence round-trip. Dependency-free.
2. **GP-PARTY** — service mostly exists; pure packet-bridge work. Dependency-free.
3. **GP-QUEST** — drives most PvE content; quest service real, needs load-on-enter + objective filters + UI packets. Dependency-free.
4. **GP-PET** — popular; entity + catch/hatch landed, needs packets + combat + persistence. Dependency-free.
5. **GP-VEND** — player economy; transfer logic landed, needs packets + autotrade persistence. Dependency-free.
6. **GP-BUYSTORE** — pairs with vending. Dependency-free.
7. **GP-CASHSHOP** — buy path landed, needs catalog data + point persistence + UI packets. Dependency-free.
8. **GP-AUCTION** — economy; map-side wiring landed, needs packets + item fidelity. Dependency-free.
9. **GP-ACHIEVE** — pairs with quest UI packets. Dependency-free (shares PACKET work with GP-QUEST — coordinate).
10. **GP-GUILD** — large packet set; service exists. Dependency-free.
11. **GP-HOMUN** — entity slice landed, needs AI/combat/growth/hunger/packets. Dependency-free.
12. **GP-MERC** — entity slice landed, needs AI/combat/lifetime/packets. Dependency-free.
13. **GP-ELEM** — lifetime sweep landed, needs AI + create/load/delete IPC. Dependency-free.
14. **GP-INSTANCE** — lifecycle landed; **must build the dynamic-map subsystem** (the hard prerequisite) before instances are enterable. Largest gameplay ticket.
15. **GP-WOE** — scheduler landed; needs castle Emperium/guardian content + can-hit gate. Soft-depends on GP-GUILD (castle ownership) + GP-INSTANCE patterns.
16. **GP-MVPFAME** — MVP reward packets + fame ranking board. Soft-depends on GP-PARTY (kill credit fan-out).

Phase B (infra + mobai) can be pulled **in parallel** any time a contributor wants a
smaller, self-contained vertical — none of them block Phase A.

Then Phase C (status) → D (skills) → E (combat) → F (scripting), per the standing
directive. Within each, take the first dependency-free row; `SK-ENGINE` should lead the
skills phase (it unblocks the family tickets).

## Progress log

Update as cards move (date · ticket · todo→inprogress / inprogress→done · one line).

- **2026-06-05** — **SC-MAGNITUDE** inprogress→done (Phase C status, ~15 conversion turns). Worklist 159→134 (≈25 SCs converted to real rAthena magnitudes); every *systematic* magnitude-bug seam swept — SC-02 all-6-stat mis-maps, the worklist-accounting prune, the Watk/Matk/Batk wrong-stat sweep (Merc/Doram/Soul/Izayoi/Soulfairy/ShieldspellAtk/Inspiration), wrong-sign debuffs (AllStatDown/Stomachache/BananaBomb), fixed-magnitude (Battleorders/Cheerup), clan flat-pool (×4 + Crossbowclan stat-mapping fix), and the absolute-DEF-override cluster (Eternalchaos/Barrier/Keeping). The recalc-persistence axis spun out to its own ✅ SC-DERIVED-RECALC. Remaining residue (skill-set-magnitude + multi-system elemental SCs) filed as SC-MAGNITUDE-EDGE. **Unblocks SC-FAMILIES.** Full Map.Server.Tests 4688 pass (1 standing replay-fixture).

- **2026-06-04** — **SC-DERIVED-RECALC** inprogress→done (Phase C status, 5 batches). Swept a systemic silent-persistence bug: `Register` doesn't merge OnRecalc and the generator only attaches OnRecalc to handlers it synthesizes, so every *bespoke* handler that modified a CalcPc-reset stat (Watk/Matk from the SC-MAGNITUDE turns 8-10 sub-sweep, plus Def/Def2/Mdef/Mdef2/Hit/Flee/Flee2/Cri/Batk/Patk/Smatk/Res/Hplus/Crate here) lost its bonus on the first recalc. ~55 handlers given a derived-field OnRecalc that re-applies ONLY the reset fields (primary stats ride the COMBAT-10 param-base delta; AspdRate isn't reset; MaxHp/MaxSp use OnRecalcPool) — including the percent-of-base (GtChange/Fleet/Magicpower/Neutralbarrier), primary-coupled (Truesight), and bit-packed multi-axis (Berserk) cases. Final audit: 0 bespoke handlers modify a reset field without OnRecalc. Verified via the Combat53BespokeRefoldTests recalc-survival theory (107 cases) + bespoke unit tests; full Map.Server.Tests 4665 pass (1 standing replay-fixture).

- **2026-06-04** — **GP-ACHIEVE** inprogress→done (Phase A, 1 turn; parallel gameplay worktree, merged into the status-epic main). Built the achievement client packet bridge + title system + load-on-enter on top of the FEATURE-04 service. Fleshed the four ZC packets out of opaque-byte placeholders into real rAthena field shapes (ZC_ALL_ACH_LIST 50B/entry, ZC_ACH_UPDATE 66B, ZC_REQ_ACH_REWARD_ACK 7B, ZC_ACK_CHANGE_TITLE 7B) + two new CZ packets (CZ_REQ_ACH_REWARD, CZ_REQ_CHANGE_TITLE) + two handlers. Service: PcLogin, EmitUpdate, SetTitle (ownership-gated equip/clear + clif_name_area re-broadcast), LevelInfo/TotalScore. **Parity fix:** removed FEATURE-04's auto-grant-on-completion — rAthena flags completion + sends clif_achievement_update only; the reward is the manual CZ_REQ_ACH_REWARD claim. Load-on-enter via IntifService.AchievementRequestAsync; equipped title_id persists via a new CharacterDataResponse.title_id proto field (existing char title_id column, no migration) + PlayerEntity.TitleId. 37 tests (4 reworked + 33 new); Map.Server.Tests 4539 pass + Char.Server.Tests 176 pass (the 1 standing replay fixture + 5 environmental npm-bundle tests are env-only). Filed GP-ACHIEVE-NONBATTLE-TRIGGERS + GP-ACHIEVE-REWARD-SCRIPT (both SCR-PLAYER).

- **2026-06-04** — **SC-DERIVED-RECALC** new→inprogress (Phase C status; filed from the SC-MAGNITUDE recalc audit, batch 1 done). Root cause: `Register` doesn't merge OnRecalc and the generator only adds OnRecalc to handlers it synthesizes, so 45 bespoke handlers that modify a CalcPc-reset derived field (Def/Hit/Flee/Cri/Batk/…) lose their bonus on recalc (confirmed: Truesight fails the Combat53 refold assert). Batch 1: Fear (Hit/Flee −20%) + Cloaking (Cri) given OnRecalc, verified. 43 remain (percent/primary-coupled ones need per-field care).

- **2026-06-04** — **SC-INSPIRATION-RECALC** todo→done (Phase C status; filed + closed within the SC-MAGNITUDE audit loop). SC_INSPIRATION had three bugs: val2 was added to Batk (status.cpp:7141/7224 + status.yml say Watk+Matk), MaxHp was a flat +4·Val1 (status.cpp:3170 adds it as a percent), and it had no recalc re-application. Fixed all three (OnRecalc re-applies Watk+Matk; OnRecalcPool re-folds MaxHp %; primary-stat +Val3 survives via the COMBAT-10 param-base delta so it's applied once). 3 tests; full Map.Server.Tests 4592 pass (1 standing replay-fixture).

- **2026-06-04** — **SC-IMMUNE** inprogress→done (Phase C status, 2 turns; user-directed loop over todo/status). The SC-08 P0.5 leaves. Turn 1: wired the existing-but-uncalled status_change_refresh into the weapon-swap path (EquipService.TryRecalcStats → IStatusChangeService.Refresh, re-resolves the Fire/Earth/Wind/Waterweapon endow on a weapon change) + replaced IsDisabledOnMap's per-call O(N) GetHashCode scan with a cached mapId→name lookup. Turn 2: the bResEff effect-resist card matrix — new EquipBonusBundle.ResEff (StatusType→1/100% resist) parsed from bonus2 bResEff via BonusScriptExtractor, applied in GetScDef to the SC landing rate (and, renewal, the duration) per rAthena sd->reseff/status_get_sc_def. A player with an effect-resist card now resists the matching SC by the card amount; a weapon swap under an endow re-resolves the element; nostatus maps still block SCs (via the cached lookup). 4 tests (3 reseff + 1 weapon-swap-refresh); full Map.Server.Tests 4555 pass (1 standing replay-fixture). Companion (homun/merc/elem) calc refresh on level-up → filed SC-COMPANION-CALC (genuinely blocked on the companion lifecycle tickets GP-HOMUN/GP-MERC/GP-ELEM).

- **2026-06-04** — **AI-CHANGECHASE-VIS** inprogress→done (Phase B mobai, 1 turn; user-directed loop over todo/mobai — **the AI epic is now fully complete, todo/mobai is empty**). The changechase retarget now honours rAthena's status_check_skilluse visibility guard: TryChangeChase gained an optional canPerceive predicate, and MobAiService supplies the hide/cloak gate (EntityActionGates.CanSee — the SC hiding set with boss/detector pierce) + the LOS path check the aggressive scan uses, so a chasing MD_CHANGECHASE mob skips a hidden / wall-blocked enemy in melee reach. 2 tests (hidden-skipped + visible-switched); full Map.Server.Tests 4551 pass (1 standing replay-fixture).

- **2026-06-04** — **AI-PERF** inprogress→done (Phase B mobai, 1 turn; user-directed loop over todo/mobai — completes the AI epic). Mostly a verify ticket (the cell-grid range scans + most slave coupling landed in MOBAI-01/03/04). Confirmed every mob-AI *range* scan already routes through the bucketed cell-grid (EntityRegistry.ForEachInRange → MapSpatialIndex): aggro scan, TryChangeChase, PickRandomEnemy, RetargetMobsChasing, slave friend-search (the remaining .All() walks are tick dispatch / CountSlaves / spotted-cleanup — not range scans). Two real fixes: (MOBAI-07) the changechase now sets the in-reach enemy DIRECTLY (mob_ai_sub_hard_changechase does md->target_id = bl->id with no mob_can_changetarget) — removed the wrong CanChangeTarget gate in MobAiService, so a RUSH-state MD_CHANGECHASE mob switches even without MD_CHANGETARGETCHASE; (MOBAI-06) slave target-inheritance now accepts the master's target of any type (was PlayerEntity-only), so a player-summoned slave joins its master against a MOB target. 2 regression tests; full Map.Server.Tests 4549 pass (1 standing replay-fixture). Filed AI-CHANGECHASE-VIS (the changechase status_check_skilluse visibility sub-gate). **All todo/mobai (the AI epic) is now done.**

- **2026-06-04** — **AI-BOSS-ACTIVE-HP** inprogress→done (Phase B mobai, 1 turn; user-directed loop over todo/mobai). Two boss-flavour AI items split from AI-MVP. (1) The boss/mob active-window: rAthena's real mechanism is time-based (mob_active_time / boss_active_time, battle.cpp default 5000 ms), not a wider radius — MobAiService.Tick now records last_pcneartime when a PC is in view, and the lazy/hard gate (extracted to the internal ShouldRunLazy) keeps a mob on the HARD path for that window after the last PC leaves view (boss_active_time for MD_STATUSIMMUNE). (2) The floating mob HP bar: new ZC_HP_INFO (0x0977 <id>.L <hp>.L <maxHP>.L) emitted from DamageService (clif_monster_hp_bar, gated by monster_hp_bars_info default-on) to nearby players when a damaged mob is below max HP (skipped on the killing blow). 5 tests (3 active-window boundary + 2 HP-bar); full Map.Server.Tests 4547 pass (1 standing replay-fixture).

- **2026-06-04** — **AI-MVP** inprogress→done (Phase B mobai, 1 turn; user-directed). The deliverable was one missing loader: the entire mob-skill-cast pipeline (MobSkillCastService picker + condition registry + target resolver + mob chat + slave summon) was already built and wired into the AI tick, but MobDbEntry.Skills was ALWAYS empty because nothing loaded mob_skill_db — so no mob (MVP or trash) ever cast a skill. Added the loader: MobDb.LoadSnapshot joins IMobSkillDbRepository and attaches each mob's skill rows in seeded/priority order via MobSkillRowMapper (rAthena state/condition/target strings → pinned enums: attack→Berserk, chase→Rush, randomtarget→Random, myhpltmaxrate/slavele/rudeattacked/… conditions, cond2/emotion/chat payload). MVPs now cast by priority, announce on HP thresholds (myhpltmaxrate + chat), and summon/heal/teleport — on the existing already-tested execution path; the MVP drop tier was already live in MobDeathObserver. This unblocks ALL mob skills, not just MVPs. 16 loader/mapper tests; full Map.Server.Tests 4542 pass (1 standing replay-fixture). Filed AI-BOSS-ACTIVE-HP (boss wider active-radius + floating boss-HP bar — the archived MOBAI-02's residual boss-AI items, not in this ticket's done-criteria).

- **2026-06-04** — **GP-AUCTION** inprogress→done (Phase A, 3 turns). Built the auction house client packet bridge + full-item fidelity + mail delivery + expiry sweep (the map-side escrow/dispatch service was archive FEATURE-06). Turn 1 — the client packet bridge: 13 packets (8 CZ stage/register/cancel/bid/buy-now/search/my-info/open + 5 ZC message/results/setitem-ack/close-ack/open-window), IAuctionClientService emit hub + 8 handlers, session staging state, fee/zeny pre-gates for the specific not-enough-zeny messages, search + my-info share the 83-byte results emit. Turn 2 — full-item fidelity: added MailAttachmentItem to the AuctionData proto, the map populates it from the escrowed item + reads cards back into the results; char side persists cards/options onto the auction row + delivers the item (full-fidelity mail attachment) + zeny on close (item→winner, bid→seller), instant buy-now-via-bid (buy-now→seller, overage refunded), and cancel (item→seller return); category search buckets (types 0-3); fixed the EF-untranslatable AuctionRequestList filter. Turn 3 — the expiry sweep: shared AuctionDelivery helper + CharMaintenanceService.TickAuctionExpiry (1-min, char game-loop driven) ends auctions past their end time, delivering to the winner / returning to the seller by mail. A player registers a carded item (fee paid, escrowed), others browse by category + bid (a higher bid mails the prior bidder a refund), and at expiry (or buy-now) the winner receives the carded item + the seller the zeny by mail — all surviving logout. 26 map auction-suite + 11 char auction tests (fidelity/delivery/category/expiry); Map suite 4526 pass, Char suite 175 pass (1 standing replay-fixture).
- **2026-06-04** — **GP-CASHSHOP** inprogress→done (Phase A, 2 turns). Built the cash-shop client packet bridge + data + account-bound point persistence (the BuyList purchase engine was archive FEATURE-13). Turn 1 — data: rAthena ships db/item_cash.yml empty (admins fill db/import/), so the importer now falls back to a project default catalog of real items (7 tabs, 11 rows) regenerating seed_item_cash.sql; packets (11 new): CZ open/list/close/buy (0x0b6d/0x08c9/0x084a/0x0848) + ZC open-balances/scheduler-list/buy-result (0x0a2b/0x08ca/0x0849, CASHSHOP_RESULT_*) + sale selling/amount (0x09b2/0x09c4); ICashShopClientService emit hub, CatalogTabs()/ActiveSaleNotifications() accessors, SaleNotifyLogin emit, Open/List/Close/Buy handlers (trading gate + kafra-cash split). Turn 2 — persistence: #CASHPOINTS/#KAFRAPOINTS are acc_reg_num registers (rAthena pc_readaccountreg/pc_setaccountreg), so no new proto/table — new CashPointsReg helper (mirrors DieCounterReg) hydrates on map-enter (NotifyActorInitHandler) + mirrors live balances on save (PlayerStateService), riding the existing account var-reg pipeline. A player opens the cash shop, browses tabs, buys with a kafra-then-cash split (item granted, success ack, sale-tab discounted price), is rejected on insufficient points/full inventory, and relogs with the remaining balance intact (account-bound). 25 cash-shop-suite tests (19 bridge/service + 6 persistence); full suite 4516 pass (1 standing replay-fixture). Filed GP-CASHSHOP-SLOT-WEIGHT-CODE (slot-vs-weight reject code) + GP-CASHSHOP-SALE-BANNER (timed-sale @sale/persistence/refresh subsystem).
- **2026-06-04** — **GP-BUYSTORE** inprogress→done (Phase A, 2 turns). Built the buying-store client packet bridge (the escrow/transfer/refund service was archive FEATURE-12): turn 1 — open/create (CZ_REQ_OPEN_BUYING_STORE, escrow the buyer's zeny up to the limit) + close-refund + owner item list (ZC_MYITEMLIST_BUYING_STORE) + stall sign on-map (ZC_BUYING_STORE_ENTRY, AOI) + open-fail + disappear + escrow/refund par-change. Turn 2 — click-to-view (CZ_REQ_CLICK_TO_BUYING_STORE → ZC_ACK_ITEMLIST_BUYING_STORE visitor offer list) + sell-in/trade (CZ_REQ_TRADE_BUYING_STORE → buyingstore_trade: item→buyer, escrow→seller; rAthena gates store-id→DealFailed / unwanted+overcount→OverCount / escrow-short→BuyerLacksZeny / buyer-full→silent; per-item seller delete + buyer amount-update + buyer pickup + seller SP_ZENY; auto-close+refund when escrow exhausted or all offers filled). A player opens a buying store paying Nz for an item, escrowing the limit; others see the stall, click to view its offers, and sell matching items in (paid from the held escrow); closing refunds the remainder. 16 buyingstore-suite tests; full suite 4501 pass (1 standing replay-fixture). Search ➡️ INF-SEARCHSTORE (universal market search, shared with GP-VEND); autotrade ➡️ GP-AUTOTRADE-RUNTIME.
- **2026-06-03** — Board restructured from layer-sliced to vertical. Old board (112 todo
  + 119 done) archived under `_archive/`; new TEMPLATE enforces end-to-end scope. The
  landed code from the archived `FEATURE-01..15` / `COMBAT-01..96` / `SC-01..08` etc.
  remains in the repo and is cited by the vertical tickets as "verify-and-extend".
- **2026-06-03** — **GP-MAIL** inprogress→done (Phase A, 6 turns). Built the entire RODEX client packet bridge to rAthena struct fidelity + handler unit tests (the service + persistence IPC were already built in archive FEATURE-05): receive side (open-mailbox/refresh→ZC_ACK_MAIL_LIST, read→ZC_ACK_READ_RODEX, zeny/item-from-mail→acks, delete→ack) + compose side (begin-write/check-name/add-item/remove-item/send→acks) + the service methods (RequestInbox/Read/Delete/CheckReceiver) + the overweight gate. A player can compose a mail with zeny+a carded item, send it, and the recipient opens RODEX, reads, claims (cards intact), and deletes it — all client→service→client. 16 handler tests + extended service tests; full suite 4414 pass (1 standing replay-fixture). Filed GP-MAIL-RENTAL (rental expiry) + GP-MAIL-PARTIAL-CLAIM (separated claims); live-client wire validation is the project's standing deferred pass.
- **2026-06-03** — **GP-PARTY** inprogress→done (Phase A, 3 turns). Built the party client packet bridge (the char IPC + notify layer + cache were already present): create (CZ_MAKE_GROUP) + invite-by-name (CZ_PARTY_JOIN_REQ) + leave/expel/change-leader/change-option handlers (driving the established IIntifService path) + the HP-bar/minimap-dot sync (new ZC_NOTIFY_HP_TO_GROUPM + PartySyncService, rAthena party_send_xy_timer cadence, wired into MapServerImpl). A player can create/invite/accept/leave/expel/change-leader/set-EXP-share and see teammates' HP bars + dots. 19 party-suite tests; full suite 4429 pass (1 standing replay-fixture). Filed GP-PARTY-EXPEL-REASON + GP-PARTY-INSTANT-HP.
- **2026-06-04** — **GP-VEND** inprogress→done (Phase A, 3 turns). Built the vending client packet bridge (the buyer↔vendor transfer was archive FEATURE-11): open from cart (CZ_REQ_OPENSTORE2, offers validated against the live cart) → the vendor's own item list (ZC_PC_PURCHASE_MYITEMLIST) + stall sign on-map (ZC_STORE_ENTRY, AOI) + open ack; browse (CZ_REQ_VENDING_ITEMS → ZC_PC_PURCHASE_ITEMLIST_FROMMC price list + vended-id anti-desync); buy (CZ_PC_PURCHASE_ITEMLIST_FROMMC → PurchaseReq with the rAthena result codes + vendor sale report + buyer item-pickup + SP_ZENY par-change for both); sold-out auto-close + stall disappear. A player opens a shop from their cart, others see the stall, a buyer browses + buys, and the shop auto-closes when sold out. ~18 vending-suite tests; full suite green (1 standing replay-fixture). Filed GP-VEND-OVERWEIGHT (weight gate) + GP-AUTOTRADE-RUNTIME (the offline-shop headless runtime, shared with GP-BUYSTORE).
- **2026-06-04** — **GP-PET** inprogress→done (Phase A, 7 turns). Built the entire pet client packet bridge + persistence round-trip (the service/entity layer was archive FEATURE-07): pet-menu (CZ_COMMAND_PET) + status/changestate emits, the real click-to-tame capture (CZ_TRYCAPTURE → pet_catch_process_end with the live-HP% rate — parity fix off the prior mob-death model) + roulette/start-capture, the egg-list hatch (IT_PETEGG use → ZC_PETEGG_LIST → CZ_SELECT_PETEGG → BirthProcess), rename (CZ_RENAME_PET) + emotion (CZ_PET_ACT/ZC_PET_ACT), the looter-pet loot bag (SummonAiService pet_ai_sub_hard loot branch + pet_lootitem_drop deposit), and the pet_id↔egg-card persistence round-trip (catch→PetCreate→bound egg via CARD0_PET + card-aware GiveItemWithCards; hatch→PetLoad→hydrate intimacy/hunger/name). Fixed a bug where catching created the char pet row but never granted the player an egg. A player tames a mob, hatches the bound egg into the saved pet, feeds/renames/emotes/loots/returns it, and relogs to re-hatch the same pet. ~40 pet-suite tests; full suite 4480 pass (1 standing replay-fixture). Filed GP-PET-CATCH-GATES, GP-PET-RENAME-NAMEPKT, GP-PET-LOOT-OVERFLOW, GP-PET-AUTOSKILL (scripting), GP-PET-LOGIN-RESUMMON, GP-PET-LOYALTY-BONUS (scripting).
- **2026-06-03** — **GP-QUEST** inprogress→done (Phase A, 5 turns). Built the quest client packet bridge + load-on-enter + filters (the quest service state-machine + persistence IPC were archive FEATURE-03/02): ZC_DEL_QUEST/ZC_UPDATE_MISSION_HUNT (live hunt counter) → ZC_ADD_QUEST/+MISSION (quest appears) → ZC_ALL_QUEST_LIST login snapshot + load-on-enter (IIntifService.QuestRequestAsync → Hydrate → PcLogin at LoadEndAck) → CZ/ZC_ACTIVE_QUEST toggle + immediate-save (IQuestSaveTrigger, chrif_save parity) → FEATURE-21 any-mob objective filters (QuestDbEntity +21 filter cols + migration + importer/seed regen of 4826 quests; UpdateMobObjective(QuestMobContext) runs rAthena's 7-check race/size/element/level/location/allow-list gate). A player accepts a quest, sees it with live 0/N progress that ticks on kills (incl. "kill N Fish-type" filter quests), completes it, toggles tracking, and relogs mid-hunt with progress intact. 38 quest-suite tests; full suite 4444 pass (1 standing replay-fixture). Filed GP-QUEST-FILTER-INSTANCE + GP-QUEST-FILTER-DISPLAY.
