# T6 — login / char / inter audit-doc rollup · 2026-05-22

Companion to the T5 audit pass ([README history](README.md#history)).
T5.2 closed every `map/*-parity.md` to 0 ❌; this is the same audit
sweep extended to the **login / char / inter** trees.

## Acceptance grep

The goal's acceptance criterion is:

```bash
find .agents/migrations -name "*.md" \
  | xargs awk '/^\| / && /❌/' \
  | grep -v "Bucket\|❌ missing\|❌ stub\|Legend\|0 ❌"
```

Run on **2026-05-22 against the login/char/inter trees specifically**:

```bash
find .agents/migrations/{login,char,inter} -name "*.md" \
  | xargs awk '/^\| / && /❌/'
```

Result: **empty**. Every status-table row in every login / char / inter
doc is ✅, ⚠️, or 🔁 — no stale ❌ markers.

## Per-file tally (2026-05-22)

| File | ❌ rows | Notes |
|---|---|---|
| `login/status.md` | 0 | All knobs closed by L-H1 / L-M1 / L-M2 / L-M3 / L-M4 (tasks #48 + #50–55, landed 2026-05-19) |
| `char/grpc.md` | 0 | All gaps closed by C-H1 / C-H2 / C-M1 / C-M2 / C-M3 (tasks #46–47 + #49 + #52–53) plus P1 data-loss waves (mail / auction / homun) |
| `char/packets.md` | 0 | All 17 client handlers ✅; two divergences resolved as deliberate / not-real in P2 |
| `char/connect-flow.md` | 0 | Cross-server dup-online wired in P3; P4 extracted `ResolveKickTargetServerId`; pincode state machine fully aligned to rAthena in P2 |
| `inter/base.md` | 0 | P5 closed all routing (broadcast / whisper / name-change / address-sync) via `IMapServerIpcService` fan-out |
| `inter/modules.md` | 0 | Char-side DB-backing complete for all 12 modules (party / guild / storage / mail / auction / quest / achievement / pet / homun / merc / elem / clan); P1 + P8 cascade fixes closed the remaining persistence gaps |

## Cross-reference — waves → doc closures

| Wave | Tasks | Doc rows closed |
|---|---|---|
| **L-H1** | #48 | `login/status.md` `login_online_data_cleanup` timer |
| **L-M1** | #50 | `login/status.md` VIP timeout automatic downgrade |
| **L-M2** | #51 | `login/status.md` `allowed_regs` + `time_allowed` rate limit |
| **L-M3** | #54 | `login/status.md` `client_hash_check` linked-list enforcement |
| **L-M4** | #55 | `login/status.md` `loginlog.rcode` differentiation |
| **C-H1** | #46 | `char/grpc.md` starter-item insert on char create |
| **C-H2** | #47 | `char/grpc.md` `LoadHotkeys` / `SaveHotkeys` RPC + wiring |
| **C-M1** | #49 | `char/grpc.md` `char_online_data_cleanup` timer |
| **C-M2** | #52 | `char/grpc.md` `char_new_display` flag |
| **C-M3** | #53 | `char/grpc.md` `char_name_min_length` honored |
| **P1** | (pre-2026-05-15) | `char/grpc.md` + `inter/modules.md` — mail attachment / auction refund / homun skills round-trip |
| **P2** | (pre-2026-05-15) | `char/packets.md` + `char/connect-flow.md` — pincode states, reject codes, two deliberate divergences |
| **P3** | (pre-2026-05-15) | `login/status.md` + `char/connect-flow.md` — `IsAccountOnlineAnywhere` + cross-server kick |
| **P4** | (pre-2026-05-15) | `char/connect-flow.md` — `ResolveKickTargetServerId` helper |
| **P5** | (pre-2026-05-15) | `inter/base.md` — broadcast / whisper / name-change / address-sync routing |
| **P6** | (pre-2026-05-15) | (map IPC integration — out of scope here, see `map/ipc-integration.md`) |
| **P7** | (pre-2026-05-15) | (multi-server harness — `AuthNodeReplayTests`) |
| **P8** | (pre-2026-05-15) | `inter/modules.md` — PartyLeave leader cascade, GuildBreak cascade, MercenarySave cooldowns, MercenaryDelete cascade |

## Drift in top-level rollup docs

The per-tree docs are clean; the only stale ❌ rows in the project
live in **rollup tier scoreboards** that pre-date the T5 push:

| File | Stale rows | Reason |
|---|---|---|
| `PARITY-CLOSURE-ROADMAP.md` | T4 (IPC + persistence), T5 (per-file deep audits), T6 (endgame content) | All three closed by T5.4 (`12ca762..bc39af0`) + T5.2 (`95ce4a5..f9bd8d9`) + T5.5 (endgame systems task #202). Tier scoreboard needs refresh. |
| `map/scripting/README.md` | Phase 2–10 roadmap rows | **Out of scope** — these are forward-looking roadmap items for the NPC scripting subsystem, not parity-tracking rows. The TypeScript + Jint pivot (2026-05-17) explicitly stages this as multi-phase work. |

T6.5 closes the `PARITY-CLOSURE-ROADMAP.md` drift; the `map/scripting/`
phase rows stay as roadmap items per the project's own conventions
(roadmap-style ❌ ≠ parity-style ❌).

## Conclusion

The login / char / inter trees are at **0 ❌** as of 2026-05-22.
T6.2 / T6.3 / T6.4 each append a "verified" history entry to the
respective per-tree docs so future audits have an explicit checkpoint
to anchor against. T6.5 refreshes the top-level tier scoreboard.

No code changes in this tier — documentation only.
