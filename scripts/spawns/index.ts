// Mob spawn registrations. Empty in Phase 1 — the 2,950 declarative spawns
// from rAthena live in the `mob_spawn` DB table and are loaded by
// MobSpawnService at boot. registerSpawn() is available for new content.

export {};
