# <filename>.cpp parity · YYYY-MM-DD

`src/map/<filename>.cpp` (<line count> lines, <function count> unique
`<prefix>_*` functions) — <one-line subsystem description>. This audit
groups the function list by subsystem and tracks our C# coverage.

## Status legend

- ✅ implemented — full or near-full parity with rAthena
- ⚠️ partial — exists but has gaps documented inline
- ❌ missing — no C# equivalent

## Subsystem coverage

### <Subsystem 1>

| rAthena fn | Status | C# location / note |
|---|---|---|
| `<fn_name>` | ❌ | <where it would live + what's missing> |

### <Subsystem 2>

| rAthena fn | Status | C# location / note |
|---|---|---|
| `<fn_name>` | ❌ | <...> |

<!-- Repeat per subsystem. Group ~5–15 functions per table — bigger
     groups are unreviewable. -->

## Coverage summary

| Bucket | Done | Partial | Missing |
|---|---|---|---|
| <Subsystem 1> | 0 | 0 | 0 |
| <Subsystem 2> | 0 | 0 | 0 |
| **Totals** | **0** | **0** | **0** |

## Implementation plan

This session aims to land items below; the rest stays documented
and tracked for follow-up.

1. <wave name> — <one-line goal>
2. ...

Deferred (tracked):
- <item> — needs <dependency> first.

## History

### YYYY-MM-DD — initial audit
- Enumerated all <N> `<prefix>_*` functions.
- <done> done / <partial> partial / <missing> missing across <X> subsystems.
- <K>-phase plan documented above.
