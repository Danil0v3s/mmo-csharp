using Map.Server.Entities;

namespace Map.Server.Skills.Behaviors.Taekwon;

/// <summary>SL_KAIZEL — Kaizel (auto-rez). StatusSkillImpl port.</summary>
public sealed class Kaizel : StatusSkillImpl { public Kaizel() : base(SkillIds.SL_KAIZEL) { } }
