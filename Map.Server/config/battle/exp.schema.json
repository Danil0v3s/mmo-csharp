{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "./exp.schema.json",
  "title": "rAthena exp.conf",
  "description": "Generated by Tools.RathenaImporter from rAthena .conf. Each property\u0027s description is the inline comment from the source file.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "base_exp_rate": {
      "type": "integer",
      "description": "--------------------------------------------------------------\nrAthena Battle Configuration File\nOriginally Translated by Peter Kieser \u003Cpfak@telus.net\u003E\nMade in to plainer English by Ancyker\n--------------------------------------------------------------\nNote 1: Value is a config switch (on/off, yes/no or 1/0)\nNote 2: Value is in percents (100 means 100%)\nNote 3: The max level of classes is stored in the exp table.\nSee files db/exp.txt and db/exp2.txt to change them.\n--------------------------------------------------------------\nRate at which exp. is given. (Note 2)",
      "default": 100
    },
    "job_exp_rate": {
      "type": "integer",
      "description": "Rate at which job exp. is given. (Note 2)",
      "default": 100
    },
    "multi_level_up": {
      "type": "boolean",
      "description": "Turn this on to allow a player to level up more than once from a kill. (Note 1)",
      "default": false
    },
    "multi_level_up_base": {
      "type": "integer",
      "description": "Allow multi level up until a certain level?\nThis only triggers if multi_level_up is enabled.\nDefault: 0 (Unlimited)",
      "default": 0
    },
    "multi_level_up_job": {
      "type": "integer",
      "default": 0
    },
    "max_exp_gain_rate": {
      "type": "integer",
      "description": "Setting this can cap the max experience one can get per kill specified as a\n% of the current exp bar. (Every 10 = 1.0%)\nFor example, set it to 500 and no matter how much exp the mob gives,\nit can never give you above half of your current exp bar.",
      "default": 0
    },
    "exp_calc_type": {
      "type": "integer",
      "description": "Method of calculating earned experience when defeating a monster:\n0 = uses damage given / total damage as damage ratio\n1 = uses damage given / max_hp as damage ratio\n2 = 0 \u002B first attacker counts twice",
      "default": 0
    },
    "exp_bonus_attacker": {
      "type": "integer",
      "description": "Experience increase per attacker. That is, every additional attacker to the\nmonster makes it give this much more experience\n(eg: 5 people attack with 25 here, \u002B(25*4)% -\u003E \u002B100% exp)",
      "default": 25
    },
    "exp_bonus_max_attacker": {
      "type": "integer",
      "description": "Max number of attackers at which exp bonus is capped\n(eg: if set at 5, the max bonus is 4*bonus-per-char regardless of attackers)",
      "default": 12
    },
    "mvp_exp_rate": {
      "type": "integer",
      "description": "MVP bonus exp rate. (Note 2)",
      "default": 100
    },
    "quest_exp_rate": {
      "type": "integer",
      "description": "Rate of base/job exp given by NPCs. (Note 2)",
      "default": 100
    },
    "heal_exp": {
      "type": "integer",
      "description": "The rate of job exp. from using Heal skill (100 is the same as the heal amount, 200 is double.\nThe balance of the exp. rate is best used with 5 to 10)",
      "default": 0
    },
    "resurrection_exp": {
      "type": "integer",
      "description": "The rate of exp. that is gained by the process of resurrection, a unit is 0.01%.\nExperience calculations for the experience value * level difference of the person revived / 100 * resurrection_exp/10000 which the revived player has can be got.",
      "default": 0
    },
    "shop_exp": {
      "type": "integer",
      "description": "The rate of job exp. when using discount and overcharge on an NPC\n(in 0.01% increments - 100 is 1%, 10000 is normal, 20000 is double.)\nThe way it is calculated is (money received * skill lv) * shop_exp / 10000.",
      "default": 0
    },
    "pvp_exp": {
      "type": "boolean",
      "description": "PVP exp.  Do players get exp in PvP maps\n(Note: NOT exp from players, but from normal leveling)",
      "default": true
    },
    "death_penalty_type": {
      "type": "integer",
      "description": "When a player dies, how should we penalize them?\n0 = No penalty.\n1 = Lose % of current level when killed.\n2 = Lose % of total experience when killed.",
      "default": 1
    },
    "death_penalty_base": {
      "type": "integer",
      "description": "Base exp. penalty rate (Each 100 is 1% of their exp)",
      "default": 100
    },
    "death_penalty_job": {
      "type": "integer",
      "description": "Job exp. penalty rate (Each 100 is 1% of their exp)",
      "default": 100
    },
    "zeny_penalty": {
      "type": "integer",
      "description": "When a player dies (to another player), how much zeny should we penalize them with?\nNOTE: It is a percentage of their zeny, so 100 = 1%",
      "default": 0
    },
    "death_penalty_maxlv": {
      "type": "integer",
      "description": "Will players on max base/job level lose the EXP on death?\n0: Never lose (default as in official).\n1: Lose Base EXP.\n2: Lose Job EXP.",
      "default": 0
    },
    "disp_experience": {
      "type": "boolean",
      "description": "Will display experience gained from killing a monster. (Note 1)",
      "default": false
    },
    "disp_zeny": {
      "type": "boolean",
      "description": "Will display zeny earned (from mobs, trades, etc) (Note 1)",
      "default": false
    },
    "use_statpoint_table": {
      "type": "boolean",
      "description": "Use the contents of db/statpoint.txt when doing a stats reset and leveling up? (Note 1)\nIf no, an equation will be used which preserves statpoints earned/lost\nthrough external means (ie: stat point buyers/sellers)",
      "default": true
    },
    "use_traitpoint_table": {
      "type": "boolean",
      "description": "Use the contents of db/statpoint.yml when doing a stats reset and leveling up? (Note 1)\nIf no, an equation will be used which preserves trait points earned/lost\nthrough external means (ie: trait point buyers/sellers)",
      "default": true
    },
    "exp_cost_redemptio": {
      "type": "integer",
      "description": "EXP cost for cast PR_REDEMPTIO (Note 2)",
      "default": 1
    },
    "exp_cost_redemptio_limit": {
      "type": "integer",
      "description": "How many player needed to makes PR_REDEMPTIO\u0027s EXP penalty become 0?\nIf by default, the \u0027exp_cost_redemptio\u0027 is 1 (1%) and every single player revived the penalty is reduced to 0.2%,\nit means \u0027exp_cost_redemptio_limit\u0027 is 5.",
      "default": 5
    }
  }
}